Build an account settings panel (a single self-contained `index.html` with inline CSS, no
frameworks).

Include three grouped sections: Profile (full name, email, bio), Notifications (a few
on/off toggles for email, push, and weekly digest), and Security (current password, new
password). Each control must be clearly labelled. End with Save and Cancel buttons.

It should look clean and modern and work on mobile.

Put the whole thing in one HTML code block.
