Build an analytics dashboard home (a single self-contained `index.html` with inline CSS, no
frameworks) for a SaaS product called "Pulse".

Include: a left sidebar with the product name and navigation links; a top bar with a page
title and a user avatar; a row of four stat cards (each with a label, a big number, and a
small change indicator like "+12%"); and a recent-activity panel listing several rows.

It should look polished and lay out sensibly on smaller screens.

Put the whole page in one HTML code block.
