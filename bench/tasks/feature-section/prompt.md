Build a product features section (a single self-contained `index.html` with inline CSS, no
frameworks) for a developer tool called "Forge".

Include a short section heading and subhead, then a grid of six feature items — each with an
icon (an inline SVG or styled glyph), a title, and a two-line description. Below the grid,
add a single call-to-action button.

It should look polished and reflow sensibly on mobile.

Put the whole thing in one HTML code block.
